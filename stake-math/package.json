{
  "name": "heat-chase-stake-math",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Heat Chase: Grand Escape — Stake Engine math engine, optimizer and publish bundle",
  "scripts": {
    "generate": "tsx scripts/run.ts",
    "verify": "tsx src/verify.ts",
    "test": "vitest run",
    "check": "npm run test && npm run generate && npm run verify"
  },
  "dependencies": {
    "@bokuweb/zstd-wasm": "^0.0.27"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
