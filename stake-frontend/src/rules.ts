import { BONUS_START_RESPINS, MAX_WIN_MULTIPLIER } from "./domain";

export const GETAWAY_RULES = `Triggered by reaching a 5-star Wanted Level or landing 3 or more Armored Trucks on one paid spin. The bonus starts with ${BONUS_START_RESPINS} spins on an empty 5x4 grid. A spin that locks a new symbol keeps the remaining spin count unchanged; a spin that locks nothing uses one spin. At zero spins, all locked Gold Bar values are paid. Filling all 20 cells ends the bonus with the ${MAX_WIN_MULTIPLIER.toLocaleString()}x maximum win.`;

export const COLLECTION_RULES = "Each Beach Girl Wild reveals one piece of the current character. Pieces alone grant no gold star. Completing a character awards one gold star, up to three armed stars. The next eligible base spin uses the corresponding 1-, 2- or 3-star mode, with the same base price and 96% RTP; gold stars fill part of its Wanted meter. Ante and purchased features do not use the star mode. A natural Getaway consumes the saved stars. Completing all three characters starts a new prestige collection without erasing the third-star reward. Completed characters also unlock cosmetic frame themes. A star earned during a spin cannot change that spin's selected mode.";
